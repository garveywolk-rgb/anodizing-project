# Raw data (sacred — never edit by hand)

Data exactly as collected:
- session_YYYY-MM-DD.csv  — what I logged at the bench
- imagej_<id>.csv         — raw ImageJ exports
All cleaning happens in code and writes to ../processed/. If raw is wrong, add a note, don't overwrite.
